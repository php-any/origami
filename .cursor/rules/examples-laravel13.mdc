---
description: examples/laravel13 的项目目标与实现原则（主线）
globs: examples/laravel13/**
alwaysApply: false
---

# examples/laravel13 目标

本目录是 **官方 Laravel 13 骨架**（`laravel/laravel` ^13 + `laravel/framework` ^13），在 Origami 上逐步实现与 Laravel 生态的完整兼容。

**不是**「Laravel 风格」仿写；也**不要**回到 `examples/laravel` 那种大段 bootstrap 假实现路线。

## 原则

1. 保持官方入口：`artisan`、`public/index.php`、`bootstrap/app.php`。
2. 缺口优先修 Origami 核心（`std/`、`node/`、`data/`、`runtime/`）。
3. **禁止改 PHP 跳过错误**：不要改 `vendor/`、Livewire/Laravel/Symfony、或 `app/` 业务来消 Warning/绕过 Fatal。那是运行时语义问题。
4. `go-support/` 可做示例扩展、覆盖标准库、或实现 Laravel Request 等适配；禁止在 PHP 侧堆匿名 singleton 冒充框架。
5. 每层能力用 `tests/origami/` 冒烟验收；核心语义补 `tests/php/`。
6. 详细说明见 `examples/laravel13/ORIGAMI.md`。
7. **启动用 `go run`**：在本目录执行 `go run -mod=mod . serve --port=18086`（或 `go run -mod=mod . list`）。不要先编 `laravel13.exe`。HTTP 验收带 `curl --max-time`。

## 旧目录

`examples/laravel` 已偏离主线，仅作对照；新工作不要继续往那里加桥接补丁。
