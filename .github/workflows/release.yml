name: Release LSP

on:
  push:
    tags:
      - "v*"

jobs:
  build-lsp:
    name: Build LSP
    runs-on: ubuntu-latest
    strategy:
      matrix:
        include:
          - os: linux
            arch: x64
            goos: linux
            goarch: amd64
            ext: ""
          - os: windows
            arch: x64
            goos: windows
            goarch: amd64
            ext: ".exe"
          - os: darwin
            arch: x64
            goos: darwin
            goarch: amd64
            ext: ""
          - os: darwin
            arch: arm64
            goos: darwin
            goarch: arm64
            ext: ""
    steps:
      - uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v4
        with:
          go-version: "1.24"

      - name: Build binary
        working-directory: tools/lsp
        env:
          GOOS: ${{ matrix.goos }}
          GOARCH: ${{ matrix.goarch }}
          CGO_ENABLED: 0
        run: go build -o ../../bin/zy-lsp-${{ matrix.os }}-${{ matrix.arch }}${{ matrix.ext }} .

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: lsp-${{ matrix.os }}-${{ matrix.arch }}
          path: bin/zy-lsp-${{ matrix.os }}-${{ matrix.arch }}${{ matrix.ext }}

  create-release:
    needs: build-lsp
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/download-artifact@v4
        with:
          path: artifacts

      - name: Create Release
        uses: softprops/action-gh-release@v1
        with:
          files: artifacts/**/*
          draft: false
          prerelease: false
