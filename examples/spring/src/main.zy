namespace App;

use Net\Annotation\Application;

@Application(name: "DemoApp")
function main($request, $response) {
    // 应用注解会在 AST 首部注入隐藏逻辑：
    // 1) 通过反射扫描控制器并注册路由
    // 2) 每个请求都分配了一个独立的 vm 隔离
    // 这里无需重复编写分发逻辑

    Log::info("main处理请求 " + $request->path())
}


