<?php

namespace tests\basic;

echo "=== for 循环多语句测试 ===\n";

// 测试多个初始化语句
$i = 0;
$j = 0;
for ($i = 0, $j = 0; $i < 5; $i++, $j++) {
    // 循环体
}
if($i == 5 && $j == 5) {
    Log::info("多个初始化语句和增量语句测试通过");
} else {
    Log::fatal("多个初始化语句和增量语句测试失败，期望: i=5, j=5, 实际: i=" . $i . ", j=" . $j);
}

// 测试多个初始化语句，单个增量语句
$i = 0;
$j = 0;
for ($i = 0, $j = 10; $i < 5; $i++) {
    // 循环体
}
if($i == 5 && $j == 10) {
    Log::info("多个初始化语句，单个增量语句测试通过");
} else {
    Log::fatal("多个初始化语句，单个增量语句测试失败，期望: i=5, j=10, 实际: i=" . $i . ", j=" . $j);
}

// 测试单个初始化语句，多个增量语句
$i = 0;
$j = 0;
for ($i = 0; $i < 5; $i++, $j++) {
    // 循环体
}
if($i == 5 && $j == 5) {
    Log::info("单个初始化语句，多个增量语句测试通过");
} else {
    Log::fatal("单个初始化语句，多个增量语句测试失败，期望: i=5, j=5, 实际: i=" . $i . ", j=" . $j);
}

// 测试三个初始化语句和三个增量语句
$i = 0;
$j = 0;
$k = 0;
for ($i = 0, $j = 0, $k = 0; $i < 3; $i++, $j++, $k++) {
    // 循环体
}
if($i == 3 && $j == 3 && $k == 3) {
    Log::info("三个初始化语句和三个增量语句测试通过");
} else {
    Log::fatal("三个初始化语句和三个增量语句测试失败，期望: i=3, j=3, k=3, 实际: i=" . $i . ", j=" . $j . ", k=" . $k);
}

// 测试多个初始化语句，不同初始值
$i = 0;
$j = 0;
for ($i = 0, $j = 10; $i < 5; $i++, $j--) {
    // 循环体
}
if($i == 5 && $j == 5) {
    Log::info("多个初始化语句，不同初始值和增量测试通过");
} else {
    Log::fatal("多个初始化语句，不同初始值和增量测试失败，期望: i=5, j=5, 实际: i=" . $i . ", j=" . $j);
}

// 测试无初始化语句，多个增量语句
$i = 0;
$j = 0;
for (; $i < 5; $i++, $j++) {
    // 循环体
}
if($i == 5 && $j == 5) {
    Log::info("无初始化语句，多个增量语句测试通过");
} else {
    Log::fatal("无初始化语句，多个增量语句测试失败，期望: i=5, j=5, 实际: i=" . $i . ", j=" . $j);
}

// 测试多个初始化语句，无增量语句
$i = 0;
$j = 0;
for ($i = 0, $j = 0; $i < 5;) {
    $i++;
    $j++;
}
if($i == 5 && $j == 5) {
    Log::info("多个初始化语句，无增量语句测试通过");
} else {
    Log::fatal("多个初始化语句，无增量语句测试失败，期望: i=5, j=5, 实际: i=" . $i . ", j=" . $j);
}

echo "=== for 循环多语句测试完成 ===\n";

