<?php
// 简单的HTML for循环测试

// 测试数组
$items = ["apple", "banana", "orange"];

// 测试for循环HTML
<div for="$item in $items">
    <span>水果: $item</span>
</div>

// 测试带键值的for循环
<div for="$index, $item in $items">
    <span>第$index个: $item</span>
</div>
?> 