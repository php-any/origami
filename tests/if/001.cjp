namespace tests\if;

$data = true;

if($data) {
    Log::info("if(true) 正常");
} else {
    Log::fatal("if(true) 异常");
}

$data = false;

if($data) {
    Log::fatal("if(false) 异常");
} else {
    Log::info("if(false) 正常");
}